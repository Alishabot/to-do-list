/**
 * Entry point for the To-Do List CLI Application
 * Imports and runs the main application
 */

import { TodoApp } from './app';

// Create and run the application
const app = new TodoApp();
app.run();
